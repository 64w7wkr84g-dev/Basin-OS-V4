{
  "name": "basin-os-clean-rebuild",
  "version": "1.0.0",
  "private": true,
  "description": "Basin Ventures clean static lead-generation OS",
  "scripts": {
    "check": "node --check basin-radar-runner.js && node --check js/app.js",
    "radar": "node basin-radar-runner.js"
  },
  "engines": {
    "node": ">=20"
  }
}