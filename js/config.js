/* Basin OS v4 config.js
   Phase 2 target:
   - Move PLAYBOOK, sequence templates, thresholds, radar defaults, API labels here.
   Current v4 staging keeps runtime in app.js to avoid breaking behavior.
*/
window.BASIN_OS_VERSION = "v4.0.0-staging";
window.BASIN_OS_STORAGE_KEY = "basin_os_integrated";
